<div id='actions'>
  <ul>
    <% if Admin::RefineryFilebrowsersController.searchable? %>
      <li>
        <%= render :partial => "/shared/admin/search",
                    :locals => {
                      :url => admin_refinery_filebrowsers_url
                    } %>
      </li>
    <% end %>
    <li>
      <%= link_to t('.create_new'), new_admin_refinery_filebrowser_url,
                   :class => "add_icon" %>
    </li>
  <% if !searching? and RefineryFilebrowser.count > 1 and Admin::RefineryFilebrowsersController.sortable? %>
    <li>
      <%= link_to t('.reorder', :what => "Refinery Filebrowsers"),
                   admin_refinery_filebrowsers_url,
                   :id => "reorder_action",
                   :class => "reorder_icon" %>

      <%= link_to t('.reorder_done', :what => "Refinery Filebrowsers"),
                   admin_refinery_filebrowsers_url,
                   :id => "reorder_action_done",
                   :style => "display: none;",
                   :class => "reorder_icon" %>
    </li>
  <% end %>
  </ul>
</div>
<div id='records'>
  <% if searching? %>
    <h2><%= t('shared.admin.search.results_for', :query => params[:search]) %></h2>
    <% if @refinery_filebrowsers.any? %>
      <%= will_paginate @refinery_filebrowsers, :previous_label => '&laquo;', :next_label => '&raquo;' %>
      <ul>
        <%= render :partial => "refinery_filebrowser",
                    :collection => @refinery_filebrowsers %>
      </ul>
      <%= will_paginate @refinery_filebrowsers, :previous_label => '&laquo;', :next_label => '&raquo;' %>
    <% else %>
      <p><%= t('shared.admin.search.no_results') %></p>
    <% end %>
  <% else %>
    <% if @refinery_filebrowsers.any? %>
      <%= will_paginate @refinery_filebrowsers,
                         :previous_label => '&laquo;',
                         :next_label => '&raquo;' %>

      <%= render :partial => "sortable_list" %>

      <%= will_paginate @refinery_filebrowsers,
                         :previous_label => '&laquo;',
                         :next_label => '&raquo;' %>
    <% else %>
      <p>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      </p>
    <% end %>
  <% end %>
</div>
<%= render :partial => "/shared/admin/make_sortable",
            :locals => {
              :tree => false
            } if !searching? and RefineryFilebrowser.count > 1 %>
